[build-system]
requires = ["setuptools>=42", "wheel", "pip"]

[tool.black]
line-length = 120
target-version =  ['py37', 'py38', 'py39']
include = '''
^/(
    [^/]*.py
    | (benchmarks|build_scripts|flax|tests|tools)/.*\.pyi?
)$
'''
exclude = ''
